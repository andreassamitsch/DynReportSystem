# Reports

Production RDL files are intentionally not committed because this repository is currently public.

The server setup copies any `.rdl` files located next to the installer into
`C:\Program Files\DynReportSystem\Reports`.

For full source/version control of production reports, make the repository private first.
